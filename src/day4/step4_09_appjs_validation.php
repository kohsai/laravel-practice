<?php

/**
 * Step4-09: app.js によるリアルタイムバリデーション導入
 * ------------------------------------------------------
 * - これまでBlade内に構造だけ記述していた `aria-*` 属性や `<div id="*-error">` を活用し、
 *   JavaScriptでリアルタイムに入力チェック・エラーメッセージの表示を行う。
 *
 * ✅ 実装内容（resources/js/app.js）：
 * - 全 `input[required]` を取得し、input/blur イベントで checkValidity() を実行
 * - `aria-invalid` を true/false に設定
 * - 対応する `*-error` ブロックを `display: block / none` で制御
 * - `aria-describedby` を自動付与してアクセシビリティ強化
 *
 * ✅ 具体的な動作：
 * - メール形式が不正 → JSで即座に赤字メッセージ表示
 * - パスワードが短い → 入力中に即反応
 * - 入力欄が空でも「未入力です」とは表示しない（ブラウザのrequired属性に任せる）
 *
 * ✅ 補足：
 * - この段階ではまだ「パスワード確認欄の一致チェック」は未対応（次ステップ）
 * - Vite経由で app.js が正しく読み込まれているか `@vite()` の確認が必要
 */
