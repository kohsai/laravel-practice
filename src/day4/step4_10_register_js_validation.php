<?php

/**
 * Step4-10: register画面のパスワード一致チェック（JS対応）
 * ------------------------------------------------------
 * - 前ステップの `app.js` に追加で、パスワード確認欄（password_confirmation）の「一致判定」機能を追加。
 * - 入力中やフォーカスアウト時に、パスワードと確認用パスワードが一致しているかを検証する。
 *
 * ✅ 実装内容（app.js の追加処理）：
 * - `#password` と `#password_confirmation` の2要素を取得
 * - 一致判定ロジック：
 *     - 両方8文字以上 → 値が一致していなければ `aria-invalid="true"` を設定
 *     - `#password_confirmation-error` に "パスワードが一致しません" を表示
 * - 未入力・未満入力の場合はJSでメッセージ表示しない（HTMLの required 属性に任せる）
 *
 * ✅ 特筆すべき点：
 * - スクリーンリーダー対応（aria-describedby, aria-invalid）
 * - メッセージの自動切替と即時フィードバック
 *
 * ✅ 表示制御例：
 * - 未入力または8文字未満 → メッセージ非表示
 * - 不一致（両方入力済） → "パスワードが一致しません" を表示
 * - 完全一致 → 非表示
 */
